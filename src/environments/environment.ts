

export const environment = {
  production: false,
  baseUrl: "http://localhost:3000",
  apiUrl: "https://localhost:7059",
  optionsLangs: ['en', 'he'],
  defaultLang: 'en',
  dateFormat: 'dd/MM/yyyy',
  maxPagingClient: 20
};

// compile for production: ng build --configuration=production
// compile for test: ng build --configuration=development
